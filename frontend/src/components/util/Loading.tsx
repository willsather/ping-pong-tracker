import styles from "@/src/components/util/CenterLoading.module.scss";

const Loading = () => {
  return (
    <div className={styles.container}>
      <p>Loading</p>
    </div>
  );
};

export default Loading;
